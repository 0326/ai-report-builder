/** @alifd/next → window.Next（Fusion UMD） */
/* eslint-disable @typescript-eslint/no-explicit-any */
const N = (window as any).Next;
export default N;
export const { Button, Message, Dialog, Input, Tag, Icon, Balloon } = N;
