# 复制为 .env 并填入凭证，重启 pnpm dev 后对话搭建即接入真实 Claude 模型。
# 二选一：
ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_AUTH_TOKEN=...        # OAuth token（自动带 oauth beta 头）

# 可选：
# ANTHROPIC_BASE_URL=https://api.anthropic.com
# CLAUDE_MODEL=claude-opus-4-8
